import 'package:flutter/material.dart';

class Tokro extends StatelessWidget {
  const Tokro({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Tokro Screen')));
  }
}
